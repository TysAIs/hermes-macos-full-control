# ⛔ MOVED — this repo is archived

This project continues as **[TysAIs/mac-toolbox](https://github.com/TysAIs/mac-toolbox)** — same MIT license, same stack (macos-harness routing ladder + SOUL.md policy stamping), plus new **resource-hygiene** tooling (CDP tab-leak watchdog, zombie worker detection, RAM-bloat guards).

Install from there:

```bash
git clone https://github.com/TysAIs/mac-toolbox.git
```
